// Private Variables
SERVER_PRIVATE_KEY="{Your Server Private Key}"
WALLET_STORAGE_URL="{Storage URL where your wallet is stored}"
CHAIN="{Main or Testnet?}"
PORT="{Port the express server is running on}"
REDIRECT_URI="{Redirect URI for Microsoft SSO}"

MONGODB_URI="{Your MongoDB URI}"
MONGODB_CLUSTER_NAME="{Your MongoDB Cluster Name}"

CERT_TYPE="{Certificate Type identifier (e.g. BSVA Employee Identity)}"

JWT_SECRET="{Your JWT Secret}"

// Microsoft
MICROSOFT_CLIENT_ID="{Your Microsoft Client ID}"
MICROSOFT_CLIENT_SECRET="{Your Microsoft Client Secret}"
MICROSOFT_TENANT_ID="{Your Microsoft Tenant ID}"

// Public Variables
NEXT_PUBLIC_SERVER_PUBLIC_KEY="{Your Server Public Key}"
NEXT_PUBLIC_CERTIFIER_URL="{Your Certifier URL (the express backend)}"

NEXT_PUBLIC_EMAIL_DOMAIN_CHECK="{Your Email Domain Check (e.g. @bsvassociation.org)}"